import argparse

# 1. Créer un parser
#parser = argparse.ArgumentParser(description="Script qui salue un utilisateur")

# 2. Ajouter un argument
#parser.add_argument("nom", type=str, help="Le nom de l'utilisateur")

# 3. Lire les arguments passés en ligne de commande
#args = parser.parse_args()

# 4. Utiliser l'argument
#print(f"Bonjour, {args.nom} !")

#parser = argparse.ArgumentParser(prog='myprogram')
#parser.add_argument('--foo', help='foo of the %(prog)s program')